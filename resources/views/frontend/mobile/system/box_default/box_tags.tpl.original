<ul id="vivvo_box_{$box_id}">
	<vte:foreach item = "$tag" from = "$tag_list">
		<vte:if test="$tag.name != ''">
			<li>
				<a href="{$tag.get_href}">
					<span style="font-size:{$tag.get_fontsize}px;"><vte:value select="$tag.name" /></span>
				</a>
			</li>
		</vte:if>
	</vte:foreach>
</ul>